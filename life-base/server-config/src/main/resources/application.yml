server:
  port: 8902
spring:
  application:
    name: server-config
  boot:
    admin:
      client:
        url: http://localhost:8903/

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8901/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"